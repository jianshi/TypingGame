<Window x:Class="TypingGameUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="350" Width="525">
    <DockPanel LastChildFill="True">
        <StackPanel Orientation="Horizontal" DockPanel.Dock="Top">
            <Button Name="NewTextButton" Click="NewTextButton_Click">New Text</Button>
            <Button Name="TestButton" Click="TestButton_Click">Test</Button>
        </StackPanel>
        <ListBox Name="mainTextBox">
            <TextBlock>Abc</TextBlock>
            <!--<RichTextBox
                Name="enterTextBox"
                KeyDown="enterTextBox_KeyDown"
                TextChanged="enterTextBox_TextChanged"
                PreviewKeyDown="enterTextBox_PreviewKeyDown"
                PreviewTextInput="enterTextBox_PreviewTextInput"
                >
                <FlowDocument Name="flowDoc" PageWidth="{Binding ElementName=enterTextBox, Path=ActualWidth}" />
            </RichTextBox>-->
        </ListBox>
    </DockPanel>
</Window>
